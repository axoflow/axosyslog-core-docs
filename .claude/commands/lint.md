Run Vale on the file I'm working on (or the path I specify) and report
all style violations grouped by severity. Read the Vale config at
`.vale.ini` to understand which rules apply. Suggest fixes for each
violation but do not apply them until I confirm.

